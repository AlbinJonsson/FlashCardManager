package view;

import javax.swing.*;
import java.awt.*;

public class StudyView extends JPanel {
    public StudyView() {
        setBackground(Theme.BG);
        setLayout(new BorderLayout());
    }
}
