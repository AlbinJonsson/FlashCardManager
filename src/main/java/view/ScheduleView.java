package view;

import javax.swing.*;
import java.awt.*;

public class ScheduleView extends JPanel {
    public ScheduleView() {
        setBackground(Theme.BG);
        setLayout(new BorderLayout());
    }
}
